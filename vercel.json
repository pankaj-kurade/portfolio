{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/portfolio" }
    }
  ],
  "rewrites": [
    { "source": "^/(.*)", "destination": "/index.html" }
  ]
}
